[package]
name = "gofer_sdk"
version = "0.1.0"
edition = "2021"

[dependencies]
proto = { path = "../proto" }
serde_json = "1"
serde = { version = "1.0", features = ["derive"] }
strum = { version = "0.24", features = ["derive"] }
